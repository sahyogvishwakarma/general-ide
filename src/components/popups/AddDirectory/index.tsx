import { FileContext } from "@/contexts/FileContext";
import { useContext } from "react";

interface Props {}
const AddDirectory: React.FC<Props> = () => {
    const {} = useContext(FileContext);
  return <></>;
};
